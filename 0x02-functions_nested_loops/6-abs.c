#include "main.h"
#include <unistd.h>

/**
 * _abs - check code
 * @r: an integer output
 *
 * Description: This function returns absloute value
 * Return: absolute value
 */

int _abs(int r)
{
	if ( >= 0)
		return (r);
	else
		return (r * -1);
}
