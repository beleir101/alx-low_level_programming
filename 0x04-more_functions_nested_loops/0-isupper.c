#include "main.h"
#include <unistd.h>

/**
 * _isupper - main func
 * @c: character to be tested
 *
 *  Description: Will test for upper case and returns 1 if it is upper case
 *
 * Return: Always 1 on success
 */

int _isupper(int c);
{
	int p = 0;
	char z;

	for (z = 'A'; z <= 'Z'; z++)
	{
		if c == z;
		{
			p = 1;
		}
	}

	return (p);
}
